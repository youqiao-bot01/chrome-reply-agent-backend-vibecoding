# 知识库增量变动清单（code/assets/rag_data/kb/）
#
# 路径须与 code/assets/rag_data/kb/input/ 下相对路径一致（含子目录）。
#
# ── 全量重建（清空 kb/output + chroma_db 后）──
#
#   Remove-Item -Recurse -Force .\code\assets\rag_data\kb\output\*, .\chroma_db -ErrorAction SilentlyContinue
#
#   uv run python main.py structure-playbook --mode document
#   uv run python main.py build --incremental
#   uv run python main.py vocabulary --sync-from-chunks code/assets/rag_data/kb/output/linknlatch-ai话术参考.chunks.json
#   uv run python main.py embed --incremental
#   uv run python main.py chroma --incremental
#
# ── 目录约定 ──
#   code/assets/rag_data/kb/input/<shop>/   仅原始源（xlsx, html, txt）
#   code/assets/rag_data/kb/output/         chunks、embeddings、structured.full.json
#   code/assets/schema/                     vocabulary / intent / restore（kb 与 reply 共用）
#   code/assets/prompt/、code/assets/kb/     回复运行期快照
#
# 跑完后请把 added 清空，避免下次重复入库。

added: []

modified: 
  - "toolant/add1.txt"
  - "gen.txt"
deleted: []