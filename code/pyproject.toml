[project]
name = "hubstudio-python"
version = "0.1.0"
description = "Knowledge base builder for HubStudio RAG documents"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psycopg2-binary>=2.9.12",
    "chromadb>=1.5.5",
    "python-docx>=1.2.0",
    "python-dotenv>=1.2.2",
    "PyYAML>=6.0.1",
    "requests>=2.31.0",
    "openpyxl>=3.1.5",
    "langchain>=1.3.2",
    "langchain-community>=0.4.2",
    "langchain-text-splitters>=1.1.2",
    "pypdf>=6.12.2",
    "beautifulsoup4>=4.14.3",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pymysql>=1.1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
hubstudio-kb = "hubstudio_python.cli:main"
reply-api = "hubstudio_python.reply.interface.app:serve_fastapi"

[tool.hatch.build.targets.wheel]
packages = ["src/hubstudio_python"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
